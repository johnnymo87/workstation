# Nix
result
result-*
.direnv/

# Secrets (encrypted files are OK, but not decrypted)
secrets/*.dec.*

# Editor
*.swp
*~
.nvim.lua
.beads/

# OpenCode user configuration (managed per-machine)
.opencode/
session-ses_*.md
