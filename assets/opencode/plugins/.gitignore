node_modules/
dist/
# package-lock.json is the committed lockfile (single source of truth; consumed
# by pkgs/self-compact-plugin via importNpmLock). bun.lock is dev-only and
# gitignored to avoid dual-lock drift (workstation-g9fe).
bun.lock
