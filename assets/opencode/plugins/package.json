{
  "name": "workstation-opencode-plugins",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "npm run test:vitest && npm run test:bun",
    "test:vitest": "vitest run",
    "test:bun": "bun test test/*.spec.ts",
    "test:watch": "vitest",
    "typecheck": "npx tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.14.19"
  },
  "devDependencies": {
    "@opencode-ai/sdk": "^1.1.55",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
