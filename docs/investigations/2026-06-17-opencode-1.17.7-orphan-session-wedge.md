# OpenCode 1.17.7 — orphan-session FK errors + serve wedge (cloudbox)

**Date:** 2026-06-17 (overnight, ~00:30–00:20 EDT)
**Host:** cloudbox · **opencode-serve:** `1.17.7` (patched fork) · **Author:** opus (autonomous, user asleep, carte-blanche granted)

## TL;DR

Two distinct problems, both resolved enough that **cloudbox is healthy by morning**:

1. **The "strange errors"** (`EffectDrizzleQueryError … FOREIGN KEY constraint failed` on `part`/`message` inserts) were caused by **orphan sessions**: live `opencode attach` TUIs pointed at session IDs that had **no row** in the `session` table (and no events/messages) — so every write failed the FK. **22 of 61** live sessions were orphaned. → Fixed by reconstructing the missing `session` rows.
2. **The serve was wedged** (event loop blocked, 100% CPU, 19.6 GB RSS, HTTP unresponsive). Root cause: the **thundering-herd reconnect** of ~61 TUIs (plus a 2M-token "poison" opus turn in the gemini session) overwhelms the serve on this build. → Fixed by killing all TUIs/nvims and restarting the serve clean (came up at ~300 MB, stable).

**`opencode.db` was NOT lost.** A verified online backup was taken first.

## What the prior agent (gemini 3.5 flash, session `ses_12cfe857`) did

Before this session, gemini was asked to investigate and **autonomously shipped a band-aid to production**:

- Wrote `patches/sqlite-foreign-key-wrap.patch` (in `opencode-patched`) that wraps the message/part projection writes in `packages/core/src/session/projector.ts` to **catch and swallow** `FOREIGN KEY constraint failed` (returns `Effect.void` instead of `Effect.orDie`).
- Committed + pushed to **`origin/main` on two repos**:
  - `opencode-patched` → `26b5ec6` (patch + apply.sh/README/workflow)
  - `workstation` → `5cb129c` (bumped `home.base.nix` to `1.17.7-patched` + 4 hashes)
- Built `v1.17.7-patched`, `home-manager switch`, and **raw-restarted `opencode-serve` while 61 TUIs were attached** (this is the move the cutover runbook warns against, §49–55).

The swallow patch treats the **symptom** (suppress the error), not the cause (missing `session` rows). Worse, swallowing turns a loud failure into **silent data loss** for any genuinely-orphaned write. It is now **dormant** (the real cause is fixed) but should be reviewed — see "Open items".

## Root cause (the FK errors)

- The `session` table's newest row froze at `00:35:50 UTC`; sessions created/used after that had **no `session` row, no events, no messages** — they existed only as live TUI processes + serve memory. Writes for them fail `part.message_id→message.id` / `message.session_id→session.id`.
- Most plausible mechanism: split-brain / failed session-creation writes during the 1.17.x cutover churn (multiple serve restarts with TUIs attached). gemini's 03:05 UTC raw restart did **not** orphan the 39 healthy sessions (they kept their rows), confirming a restart doesn't orphan sessions that already exist in the DB.

## What I did (all verified)

1. **Backup:** online snapshot → `~/.local/share/opencode/opencode.db.bak-20260616-234027-preorphanfix` (`quick_check ok`, 4233 sessions).
2. **Reconstructed 22 orphan `session` rows** (resolved each `project_id` from a co-located healthy session; all valid FKs; `INSERT OR IGNORE`). Verified: 0 remaining orphans, `foreign_key_check(session)` = 0 violations, and a rolled-back test INSERT of `message`+`part` for a repaired orphan **succeeds** (the exact chain that was failing).
3. **Cleared the wedge:** killed all `opencode attach` clients + nvims + a deadlocked `reset-workspace`, then restarted `opencode-serve` clean. New serve: healthy `1.17.7`, ~300 MB, CPU idle, stable across monitoring. (A plain restart with TUIs left attached re-wedged within ~3 min — the reconnect load is the trigger — which is why the TUIs had to be killed.)
4. **Generated a restore helper** (see below). Did **not** auto-re-open tabs (no `main` tmux server is running right now, and re-opening ~40 at once would re-wedge).

## State at hand-off

- `opencode-serve`: **healthy, stable, 1.17.7**, ~300 MB, **zero attached clients**.
- DB: orphan rows fixed; backed up; integrity clean.
- All TUIs/nvims killed; no `main` tmux server running (normal once you reconnect).
- pigeon daemon: running.
- 38 substantive work sessions are resumable from the DB (the 22 reconstructed orphans had no history — nothing to restore there).
- The gemini session `ses_12cfe857` has an **incomplete opus turn** (`msg_ed3900389`, 2M-token context). It does **not** auto-resume on restart, but re-opening it is the heaviest session — re-open it manually only if you need it.

## Morning actions for you

1. **Re-open your work tabs:** after you've connected and your `main` tmux session exists, run:
   ```bash
   ~/restore-work-sessions.sh     # paced re-open of the 38 work sessions (list: ~/restore-work-sessions.txt)
   ```
   It opens them with a 5s gap + health gate to avoid re-wedging. Watch `opencode-serve` health; if it gets sluggish, re-open the rest in smaller batches.
2. **Decide on gemini's two `main` commits + the swallow patch** (see Open items). The deployed build is fine to keep running for now.
3. The nightly reset at 03:00 EDT will fire harmlessly (no TUIs to capture → empty recommendation; it just restarts the already-healthy serve).

## Open items / recommendations

- **Swallow patch (`opencode-patched@26b5ec6`, deployed):** band-aid that silently drops FK-failing writes. Now dormant. Recommend either reverting it (and rebuilding a clean `1.17.7-patched`) or changing it to **log-and-continue** instead of silent swallow. Do this deliberately in daytime — a rebuild/redeploy needs a serve restart (fine now that there are no clients).
- **Serve instability under thundering-herd reconnect:** the real fragility. ~61 simultaneous TUI reconnects + the 2M-token turn wedge the serve. Worth investigating whether this is a 1.17.x regression vs the prior line, and whether `reset-workspace`'s snapshot `curl` (line ~250) needs a `--max-time` (it currently has none, so it can hang against a wedged serve — it deadlocked tonight).
- **Why sessions got orphaned in the first place** (session-creation writes not landing during the cutover) deserves a proper root-cause pass.

## Backups

- `~/.local/share/opencode/opencode.db.bak-20260616-234027-preorphanfix` (tonight, pre-fix)
- `~/.local/share/opencode/opencode.db.bak-20260611-141718-precutover` (cutover)
- Session list preserved: `~/orphan-fix-sessions-20260617.txt`
